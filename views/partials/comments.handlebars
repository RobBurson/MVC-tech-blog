<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date createdAt}}
    </div>
    <div class="comment-text">
      {{commentText}}
    </div>
  </section>
  {{/each}}
</div>